export abstract class Lander {
    get conversations() {
        return []
    }

    getMessages(id: string) {
        return []
    }

    createNode(props, parentId: string) {}

    sendMessage(message: string, node?: any) {
        // conbine node's prompt
        // should 
    }

    
}

/**
 * what is the node?
 * it's a prmopt template that  
 */
